# These are managed by `npx convex dev` — they appear in .env.local automatically.
# Do not commit .env.local.
#
# CLERK_JWT_ISSUER_DOMAIN is set on the Convex DEPLOYMENT, not here:
#   npx convex env set CLERK_JWT_ISSUER_DOMAIN https://xxx.clerk.accounts.dev
